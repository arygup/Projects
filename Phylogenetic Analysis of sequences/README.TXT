For the following assignment:

the input was taken manually because of some random mac security issue.

Q1&2 
    A
    the distace matrix shows empty values for all "0" and not required values (x,y) & (y,x)
    B
    upon running the program the trees along with output from upgma should appear on the screen, the sequences are labled as A-J. also ref from github was used.